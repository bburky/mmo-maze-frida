{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "protocol": "inspector",
            "request": "attach",
            "name": "Attach to Frida v8 runtime",
            "port": 9229,
            "outFiles": ["${workspaceFolder}/**/*.js"],
            "sourceMapPathOverrides": {
                "*": "${workspaceFolder}/*",
            }
        }
    ]
}